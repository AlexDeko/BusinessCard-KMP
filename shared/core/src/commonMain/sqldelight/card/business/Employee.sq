CREATE TABLE employeeEntity (
    id INTEGER PRIMARY KEY NOT NULL,
    last_name TEXT NOT NULL,
    first_name TEXT NOT NULL,
    job TEXT NOT NULL
);

getAll:
SELECT * FROM employeeEntity;

saveEmployee:
INSERT INTO employeeEntity(id, last_name, first_name, job) VALUES ?;